/*
#include <stdio.h>

int main() {
    int a, b, c, d, e, f;
    scanf_s("%d %d %d %d %d %d", &a, &b, &c, &d, &e, &f);

    int det = a * e - b * d;

    int x = (c * e - b * f) / det;
    int y = (a * f - c * d) / det;
    
    printf("%d %d", x, y);
    return 0;
}
*/